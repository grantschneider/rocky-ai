[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/backends"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
